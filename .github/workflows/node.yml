name: node
on:
  workflow_dispatch:
env:
  TZ: Asia/Shanghai
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
       repository: 'nodejs/node'
       
    - name: 下载gcc
      uses: lmq8267/dl-musl@main
      with:
        target: mipsel-linux-muslsf
        static: true
        gccpath: /tmp

    - name: build
      run: |
         ./configure --help || true
         ./configure --dest-cpu=mipsel --without-snapshot --static --without-openssl
         make
        

    - uses: actions/upload-artifact@v4
      with:
        name: node
        path: bin
